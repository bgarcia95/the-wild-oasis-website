import Spinner from "./_components/Spinner";

// This will work for all loading states in the app, but you can customize it to be more specific if you want.
export default function Loading() {
  return <Spinner />;
}
